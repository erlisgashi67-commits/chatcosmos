# ChatCosmos Python Pipeline — dependencies
# Install with:  pip install -r requirements.txt
#
# Heavy ML stack (sentence-transformers pulls in torch). On a CPU-only
# machine this is fine; on GPU boxes you may want to pre-install the
# correct CUDA build of torch first.

sentence-transformers>=2.7.0
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0
tqdm>=4.66.0
